import { render, screen } from "@testing-library/react";
import mockAll from "../../../assets/mocks/mockActor.json";
import { MediaCarousel } from "../../../components/report/MediaCarousel";

/**
 * Test where we check that the MediaCarousel component renders properly.
 */
test("render MediaCarousel", () => {
  console.log("hello");
  // render!!
  // check individual cards
});
